# Ship gates

1. Confirm the branch is isolated and the worktree is clean.
2. Audit the diff for secrets, unsafe HTML, remote scripts, and unrelated files.
3. Run `npm test` and JavaScript syntax checks.
4. Run model-quality evaluation when prompt or schema behavior changes.
5. Reload and test the unpacked extension in Chrome and Vivaldi.

