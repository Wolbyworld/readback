# Readback feature workflow

Use isolated local worktrees. Read `CLAUDE.md` and `README.md` before edits. Keep agent ownership separate, combine changes on `main`, then run all tests and real browser acceptance in Chrome and Vivaldi. There is no remote push or deployment in this workflow.

